// Loaded on demand by MotionProvider so the animation feature bundle (including drag and
// shared layout) stays out of the initial JavaScript.
import { domMax } from "motion/react";

export default domMax;
